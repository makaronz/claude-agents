# Azure FSI Landing Zone Agent - Specific Dependencies

# Core dependencies (from parent requirements.txt)
# Note: claude-agent-sdk v0.1.0+ requires Claude Code CLI v2.0.1+
# Install Claude Code: npm install -g @anthropic-ai/claude-code@latest
claude-agent-sdk>=0.1.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Azure SDK dependencies
azure-identity
azure-mgmt-resource
azure-mgmt-network
azure-mgmt-keyvault
azure-mgmt-storage
azure-mgmt-authorization
azure-mgmt-policyinsights
azure-mgmt-security
azure-mgmt-monitor
azure-mgmt-managementgroups

# Optional: For enhanced Azure CLI integration
azure-cli-core

# Optional: For Terraform support
python-terraform

# Utilities
requests
aiohttp
