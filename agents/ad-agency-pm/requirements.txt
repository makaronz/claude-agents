# Ad Agency Project Manager Agent Dependencies

# Core dependencies (inherited from shared)
# These are already installed via the main repository requirements.txt

# Additional dependencies specific to this agent
pyyaml>=6.0
python-dateutil>=2.8.0

# Optional: For enhanced date/time handling
pytz>=2023.3

# Optional: For data visualization in reports
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional: For Excel export functionality
openpyxl>=3.1.0
pandas>=2.0.0

# Optional: For calendar integration (future feature)
# google-api-python-client>=2.0.0
# microsoft-graph-api>=1.0.0

# Optional: For time tracking (future feature)
# requests>=2.31.0

# Development dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0